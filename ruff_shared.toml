# The fleet's shared ruff configuration. Source of truth: agent_rules/lint/ruff_shared.toml.
# Do not edit this file in a target repo -- edit it here and run `just sync-lint-config`.
#
# This file is never read on its own: ruff does not auto-discover a file by this name.
# Each enrolled repo carries a hand-written `ruff.toml` beside it that says
# `extend = "ruff_shared.toml"` and adds whatever that repo alone needs. That per-repo
# file is NOT synced -- it is the seam this design exists to provide.
#
# Two things a per-repo ruff.toml must get right, both silent when wrong:
#   - add to `ignore` with `extend-ignore`, never `ignore`
#   - add to per-file-ignores with `extend-per-file-ignores`, never `per-file-ignores`
# The plain forms REPLACE what this file sets rather than adding to it, so the shared
# rules vanish while this file's own bytes still check out as in sync. sync_lint_config.py
# refuses to write a repo whose ruff.toml gets either wrong.

# Pinned rather than inferred. Ruff does infer target-version from `project.requires-python`,
# but only when a pyproject.toml sits beside the config it resolved -- and it falls back to
# py3.10 when one does not, silently. python-lint-hooks overrides this to py311 in its own
# ruff.toml: it publishes `ml-lints`, which the other four repos depend on, so 3.12/3.13-only
# rewrites would be wrong there.
target-version = "py313"
line-length = 120

[lint]
# ml-lints (python-lint-hooks) emits ML-prefixed codes ruff does not know about. Declaring
# them external stops RUF100 reading their `# noqa: ML001` directives as unused.
external = ["ML"]
select = ["ALL"]
ignore = [
    "COM812", # missing-trailing-comma: ruff's own docs recommend against it alongside the formatter, which already enforces this
    "D100",   # missing docstring in public module
    "D101",   # missing docstring in public class
    "D102",   # missing docstring in public method
    "D103",   # missing docstring in public function
    "D104",   # missing docstring in public package
    "D105",   # missing docstring in magic method
    "D107",   # missing docstring in __init__
    "EM101",  # raw-string-in-exception: exception must not use a string literal, assign to a variable first
    "EM102",  # f-string-in-exception: exception must not use an f-string literal, assign to a variable first
    "TRY003", # raise-vanilla-args: avoid specifying long messages outside the exception class
]

[lint.mccabe]
max-complexity = 15

[lint.pydocstyle]
convention = "google"

# Only the two path trees agent_rules itself writes into. A synced hook or script that trips
# a rule is then one edit here rather than four, and the repos that receive those files do not
# each have to work out the same ignore list. Everything else -- tests/, src/, a repo's own
# modules -- stays in that repo's ruff.toml, because agent_rules has no business ruling on it.
#
# Each tree is listed twice. These globs resolve relative to THIS FILE, not the working
# directory, and casey_ai's ruff config lives in api/ rather than at its repo root, so the
# trees sit one level up there. A `**/scripts/**` glob bridges neither layout (measured), and
# the entry that does not match in a given repo is simply inert. ar-tul moves casey_ai's config
# to its root; the `../` entries go when it lands.
[lint.per-file-ignores]
".claude/hooks/**" = [
    "INP001", # implicit-namespace-package: hooks are standalone scripts run by path, never imported
    "S603",   # subprocess-without-shell-equals-true: hooks invoke `bd` with shell=False, deliberately
    "S607",   # start-process-with-partial-path: `bd` is resolved via PATH, deliberately
]
"../.claude/hooks/**" = [
    "INP001", # implicit-namespace-package: hooks are standalone scripts run by path, never imported
    "S603",   # subprocess-without-shell-equals-true: hooks invoke `bd` with shell=False, deliberately
    "S607",   # start-process-with-partial-path: `bd` is resolved via PATH, deliberately
]
"scripts/**" = [
    "INP001", # implicit-namespace-package: dev scripts are run by path, never imported
    "S603",   # subprocess-without-shell-equals-true: driving other tools is what these are for
    "S607",   # start-process-with-partial-path: tools are resolved via PATH, deliberately
    "T201",   # print: stdout is a script's output mechanism
]
"../scripts/**" = [
    "INP001", # implicit-namespace-package: dev scripts are run by path, never imported
    "S603",   # subprocess-without-shell-equals-true: driving other tools is what these are for
    "S607",   # start-process-with-partial-path: tools are resolved via PATH, deliberately
    "T201",   # print: stdout is a script's output mechanism
]
